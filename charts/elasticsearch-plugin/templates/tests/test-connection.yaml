apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elasticsearch-plugin.fullname" . }}-test-connection"
  labels:
{{ include "elasticsearch-plugin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "elasticsearch-plugin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
